<div class="tab-pane fade active in" id="T_product_info">
  <p stlye="line-height:120%;">
    <% @item.item_images.each do |x| %>
    <img src="<%= x.path %>" align="center" class="img-responsive img-center" >
    <% end %>
  </p>
</div>
