<%= form_for :return, url: "/admin/returns/change_status", html: {method: "patch"} do |f| %>
	<%= f.hidden_field :id, :value => id %>
	<%= f.select :status, options_for_select(collection, data) %>
	<%= f.submit "Edit" %>
<% end %>